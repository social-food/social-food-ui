export enum AdminButtonVariantEnums {
    DEFAULT = 'default',
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    ACCENT = 'accent',
    ERROR = 'error',
}

export type AdminButtonVariant = AdminButtonVariantEnums;

